%h1 Grafiche

%div.box
  - DESIGNS.each do |design|
    .design
      - link = "/t-shirts/#{design.file}"
  
      %a{ href: link }
        %img{ src: "/images/designs/#{design.file}.png" }
        %p
          %a{ href: link }
            = design.name.capitalize
.clear